/* For Each(); recibe como parametro un callback que recorrerá 
el array 
No devuelve nada, su fuinción es iterar sobre cada uno de los elementos*/

let paises = ["Argentina", "Colombia", "Perú", "Ecuador"];
paises.forEach(function (elemento) {
  console.log(elemento);
});
